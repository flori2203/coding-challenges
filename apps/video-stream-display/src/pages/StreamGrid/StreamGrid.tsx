import {Grid} from '@mui/material';
import {VideoStreamGrid} from "../../features/VideoStreamViewer";

const StreamGrid = () => {
  return (
    <Grid container >
        <VideoStreamGrid />
    </Grid>
  );
};

export default StreamGrid;
